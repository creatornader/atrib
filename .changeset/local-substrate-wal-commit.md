---
"@atrib/mcp": patch
---

Allow the in-process local substrate coordinator to accept watcher-WAL commit requests with explicit receipt join metadata. Add a shared HTTP service handler for hosts that expose the same coordinator through a supervised local endpoint.
