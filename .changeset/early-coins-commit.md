---
"@atrib/mcp-wrap": patch
---

Expose `@atrib/mcp` disclosure controls in wrapper config so operators can opt wrapped MCP servers into signed `tool_name`, `args_hash`, and `result_hash` fields without writing a custom wrapper.
