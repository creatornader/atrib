---
"@atrib/recall": patch
---

Add an `include_tool_call_args` query flag so `recall_by_content` can lift raw tool_call score suppression when callers need to search indexed args and result excerpts.
