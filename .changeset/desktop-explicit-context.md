---
"@atrib/emit": patch
---

Add `ATRIB_REQUIRE_EXPLICIT_CONTEXT_ID` for hosts that should return a warnings-only response instead of signing a fresh orphan record when no `context_id` can be resolved.
