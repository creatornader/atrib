---
'@atrib/recall': patch
---

Read annotation and revision targets from signed top-level fields when building display summaries, annotation/revision aggregations, and local recall graph edges. Sidecar content remains a legacy fallback, and aggregation accepts both `topics` and `topic_tags`.
