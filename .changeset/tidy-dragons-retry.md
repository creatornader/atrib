---
'@atrib/daemon': minor
'@atrib/sdk': minor
---

Add action-bound idempotency keys for stateless daemon writes, durable result
replay across restarts, indeterminate pending outcomes, and SDK fallback
suppression after uncertain writes.
