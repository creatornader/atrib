---
'@atrib/primitives-runtime': patch
---

Report package and tool-surface contracts for every mounted primitive in HTTP health, and extend the host-owned runtime updater to build the runtime dependency closure, validate the full primitive surface, and keep recall as the only live behavioral probe.
