---
'@atrib/daemon': patch
---

Report privacy-bounded modern and legacy MCP traffic by profile, persist the
operational counters across restarts, and fail runtime updates when a
modern-only profile falls back to legacy traffic.
