# Browserbase live demo loop

## Goal

Ship a Browserbase hosted-demo layer that matches the existing Cloudflare and
Google proof pattern: a reviewer can start a fresh Browserbase MCP proof run,
see verifier receipts, and inspect explorer plus log-proof links without
getting private Browserbase session material.

## Loop shape

Milestone persistence loop. This is a bounded checklist with receipts, not an
open-ended work session.

Maximum iterations per packet: 2. Stop if the same failure repeats twice.

## Packets

### Packet 1: contract and safety checkpoint

Scope:

- Confirm the worktree state and prior proof packet commit.
- Record the loop contract and hard stop rules.

Allowed files or surfaces:

- `LOOP.md`
- read-only inspection of existing Cloudflare, Google, Browserbase, and
  Firecrawl example files.

Expected artifact or diff:

- `LOOP.md` with this contract and receipt log.

Objective checks:

- `git status --branch --short`
- `git log -1 --oneline`

Human gates:

- None.

### Packet 2: Browserbase live demo implementation

Scope:

- Add a resettable Browserbase demo service that calls the existing
  Browserbase Stagehand proof runner path.
- Keep the proof packet and demo code separate.
- Expose a proof-first UI with run status, verifier result, record hashes,
  public log indexes, explorer links, and log-proof links.

Allowed files or surfaces:

- `packages/integration/examples/browserbase-stagehand/**`
- `packages/integration/package.json`
- `packages/integration/README.md`
- `packages/integration/demo-record-surfaces.json`
- `packages/integration/test/**`
- `proof-packets/browserbase-stagehand/README.md`
- `proof-packets/CONTACT_DRAFTS.md`
- `LOOP.md`

Expected artifact or diff:

- Browserbase demo server or Worker-style app.
- Browserbase demo README.
- Script to run the demo locally.
- UI or API route that creates a fresh bounded proof run.

Objective checks:

- Focused TypeScript compile for new files.
- Focused Vitest or API smoke test.
- Redaction scan for Browserbase keys, project id, replay URLs, selectors,
  page snapshots, and action instructions.

Human gates:

- Deploying the demo to a hosted URL.
- Creating or rotating external secrets.

### Packet 3: docs and contact copy

Scope:

- Update the Browserbase contact draft to include the live demo slot.
- Keep Firecrawl as ready for a GitHub comment, not blocked on a hosted demo.
- Make the proof-packet versus live-demo distinction explicit.

Allowed files or surfaces:

- `proof-packets/CONTACT_DRAFTS.md`
- `proof-packets/README.md`
- `proof-packets/browserbase-stagehand/README.md`
- `packages/integration/examples/browserbase-stagehand/README.md`
- `packages/integration/README.md`
- `LOOP.md`

Expected artifact or diff:

- Updated Browserbase draft with hosted demo placeholder or URL if deployed.
- Firecrawl channel and draft remain exact and human-gated.

Objective checks:

- `pnpm doc-sync`
- writing-rule scan for banned prose patterns.

Human gates:

- Posting Firecrawl comment.
- Opening Browserbase issue.
- Any X, Discord, email, or direct message.

### Packet 4: commit, push, and stop decision

Scope:

- Commit accepted local changes.
- Push the feature branch as a remote checkpoint.
- Stop before merge, deploy, or public posting unless the human gate clears.

Allowed files or surfaces:

- Git metadata for this worktree and branch.
- Agent Bridge progress note after a substantive push.

Expected artifact or diff:

- One commit for the live demo work.
- Pushed branch.

Objective checks:

- `git status --short`
- `git log -1 --oneline`
- `git ls-remote --heads origin codex/mcp-platform-proof-packets-20260623`

Human gates:

- Merge to `main`.
- Deploy hosted demo.
- Public comments, issues, emails, DMs, or posts.

## Feedback gate

Acceptance must come from checks, not maker output alone:

- Test/build/lint command for code.
- API or browser smoke for the demo surface.
- Generated artifact inspection for verifier output and proof links.
- Redaction scan for private Browserbase and Firecrawl material.
- Human judgment gate for deploy, merge, and all public contact.

## Stop rules

Stop when any condition fires:

- Accepted by verifier.
- Same failure twice.
- Missing permission.
- Unclear success criteria.
- Dirty or unsafe worktree.
- Human decision needed.
- Max iterations reached.

## Anti-gaming rules

- Do not loosen checks after seeing failures.
- Do not edit success criteria to fit a partial result.
- Do not skip required tests.
- Do not claim acceptance from maker output alone.
- Do not hide unresolved blockers.
- Do not post, comment, email, DM, merge, or deploy without a human gate.

## Receipt log

| Pass | Packet | Changed                                                                                                                                                                         | Command or check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Remaining blocker                                                                                                                                                                                                                            | Next action                                |
| ---- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 1    | 1      | Created loop contract.                                                                                                                                                          | `git status --branch --short`; `git log -1 --oneline`; read Cloudflare and Google demo docs.                                                                                                                                                                                                                                                                                                                                                                                                                       | Worktree clean on `codex/mcp-platform-proof-packets-20260623` at `7fffc21`; existing patterns confirmed.                                                                                                                                                                                                                                                                                                                                                                                         | None.                                                                                                                                                                                                                                        | Build Browserbase live demo layer.         |
| 2    | 2      | Added Browserbase proof console, package script, focused test, and demo docs.                                                                                                   | `pnpm --filter @atrib/integration exec tsc --ignoreConfig --noEmit --pretty false --target ES2022 --module NodeNext --moduleResolution NodeNext --types node --allowSyntheticDefaultImports examples/browserbase-stagehand/live-demo/server.ts test/browserbase-stagehand-live-demo.test.ts`; `pnpm --filter @atrib/integration test -- test/browserbase-stagehand-live-demo.test.ts`.                                                                                                                             | Accepted: TypeScript passed; Vitest passed 3 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                              | None.                                                                                                                                                                                                                                        | Run API and browser proof.                 |
| 3    | 2      | Exercised demo server in fixture mode.                                                                                                                                          | `PORT=18788 pnpm --filter @atrib/integration browserbase-stagehand-live-demo`; `curl /health`; `curl /api/config`; `curl -X POST /api/runs`.                                                                                                                                                                                                                                                                                                                                                                       | Accepted after retry: default `8788` returned another local service; explicit `18788` returned health/config and one accepted six-step run.                                                                                                                                                                                                                                                                                                                                                      | Hosted deploy still gated.                                                                                                                                                                                                                   | Run browser proof.                         |
| 4    | 2      | Added `PORT=0` bound-port logging and ignored demo `.tmp` screenshots.                                                                                                          | Playwright click-and-receipt check against ephemeral port; screenshot saved to `packages/integration/examples/browserbase-stagehand/live-demo/.tmp/fixture-console.png`.                                                                                                                                                                                                                                                                                                                                           | Accepted after retry: first selector matched two accepted labels; tightened to `#statusChip`; Chromium verified six rows and saved screenshot.                                                                                                                                                                                                                                                                                                                                                   | Hosted deploy still gated.                                                                                                                                                                                                                   | Run doc-sync, redaction, and prose checks. |
| 5    | 2      | Seeded cached Browserbase and Gemini env vars from 1Password using cache-first files under `~/.atrib/secrets`.                                                                  | Length-only cache checks; Browserbase item field inspection with labels and lengths only.                                                                                                                                                                                                                                                                                                                                                                                                                          | Corrected Browserbase API key cache after detecting it had picked the project field. No secret values printed or committed.                                                                                                                                                                                                                                                                                                                                                                      | None.                                                                                                                                                                                                                                        | Run live demo path.                        |
| 6    | 2      | Tried the live Browserbase demo path with public log enabled.                                                                                                                   | `POST /api/runs` against live demo server on ephemeral ports `63874` and `65278`; direct upstream MCP debug between attempts.                                                                                                                                                                                                                                                                                                                                                                                      | Blocked: live run failed first at `extract`, direct upstream debug showed Gemini quota/rate-limit on the Browserbase Stagehand action path, and the second live run failed at `observe`. Same upstream failure class repeated.                                                                                                                                                                                                                                                                   | Human decision needed: wait for quota reset, provide a different model/key, or change the live Browserbase proof flow.                                                                                                                       | Stop loop under same-failure rule.         |
| 7    | 2      | Investigated the odd port behavior and changed the demo server default host to `127.0.0.1`.                                                                                     | `lsof -nP -iTCP:8788 -sTCP:LISTEN`; `lsof -nP -iTCP:18789 -sTCP:LISTEN`; `PORT=18789 pnpm --filter @atrib/integration exec tsx examples/browserbase-stagehand/live-demo/server.ts`; `PORT=0 ...`.                                                                                                                                                                                                                                                                                                                  | Root cause found: OpenClaw and local substrate owned specific loopback listeners while the demo bound wildcard `0.0.0.0`. macOS allowed both sockets, so curl hit the other service. The patched server now fails with `EADDRINUSE` on occupied loopback ports and logs the actual bound host and port.                                                                                                                                                                                          | None.                                                                                                                                                                                                                                        | Continue Browserbase live investigation.   |
| 8    | 2      | Investigated Browserbase MCP model handling and added `ATRIB_BROWSERBASE_MODEL_NAME` for self-hosted STDIO.                                                                     | `npm pack @browserbasehq/mcp@3.0.0`; inspected `dist/config.js`, `dist/sessionManager.js`, `dist/tools/observe.js`, `dist/tools/act.js`, and `dist/tools/extract.js`; direct Browserbase MCP debug with sanitized output.                                                                                                                                                                                                                                                                                          | Root cause found for self-hosted failures: direct Browserbase MCP, without Atrib, failed with Gemini `generate_content_free_tier_*` quota `limit: 0` on `observe`. `observe`, `act`, and `extract` all use Stagehand's model path.                                                                                                                                                                                                                                                               | Self-hosted STDIO cannot complete with the current Gemini key quota.                                                                                                                                                                         | Check hosted Browserbase MCP path.         |
| 9    | 2      | Checked 1Password/cache state for all provided secrets.                                                                                                                         | `op account list`; `op whoami`; length-only checks under `~/.atrib/secrets`; attempted a Firecrawl item JSON read through `op`.                                                                                                                                                                                                                                                                                                                                                                                    | Browserbase and Gemini cache files are present. Firecrawl cache file is missing. `op whoami` reports the account is not signed in, and the Firecrawl item read hung until killed.                                                                                                                                                                                                                                                                                                                | Fresh live Firecrawl reruns are blocked until the Firecrawl key is seeded into the cache from an authenticated shell.                                                                                                                        | Keep Firecrawl as fixed proof artifact.    |
| 10   | 2      | Extended `@atrib/mcp-wrap` to expose HTTP upstream config and added hosted Browserbase packet mode.                                                                             | `pnpm --filter @atrib/mcp-wrap test -- config.test.ts wrap-wiring.test.ts`; `pnpm --filter @atrib/mcp-wrap exec tsc --noEmit --pretty false`.                                                                                                                                                                                                                                                                                                                                                                      | Accepted: mcp-wrap now accepts `upstream: { type: "http", url, headers }` and passes it through to `@atrib/mcp` Streamable HTTP support. Wrapper tests passed: 2 files, 27 tests.                                                                                                                                                                                                                                                                                                                | None.                                                                                                                                                                                                                                        | Try hosted Browserbase proof.              |
| 11   | 2      | Fixed upstream diagnostics and empty private-needle handling in the packet runner.                                                                                              | Self-hosted Browserbase live probe with public log disabled; hosted Browserbase local-log probe.                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted: upstream errors now include bounded redacted diagnostics. Empty optional env values no longer trip the private material scanner. Hosted Browserbase local-log proof passed all six steps.                                                                                                                                                                                                                                                                                              | None.                                                                                                                                                                                                                                        | Generate public Browserbase packet.        |
| 12   | 2      | Generated the accepted Browserbase public proof packet through hosted Browserbase MCP.                                                                                          | `BROWSERBASE_API_KEY=$(< ~/.atrib/secrets/browserbase-api-key) ATRIB_BROWSERBASE_STAGEHAND_LIVE=1 ATRIB_BROWSERBASE_UPSTREAM=hosted ATRIB_PACKET_PUBLIC_LOG=1 ATRIB_PACKET_WRITE_ARTIFACTS=1 pnpm --filter @atrib/integration browserbase-stagehand-packet`.                                                                                                                                                                                                                                                       | Accepted after one transient hosted-capacity retry: six records verified with public log indexes `64364`, `64365`, `64366`, `64367`, `64369`, `64370`; artifacts written to `proof-packets/browserbase-stagehand/`.                                                                                                                                                                                                                                                                              | There is a partial public record at the skipped index from the failed hosted `extract` attempt.                                                                                                                                              | Verify live demo server path.              |
| 13   | 2      | Tested the live demo server against hosted Browserbase MCP with public log disabled.                                                                                            | `ATRIB_BROWSERBASE_DEMO_MODE=live ATRIB_BROWSERBASE_UPSTREAM=hosted ATRIB_BROWSERBASE_DEMO_PUBLIC_LOG=0 PORT=0 pnpm --filter @atrib/integration exec tsx examples/browserbase-stagehand/live-demo/server.ts`; `curl -X POST /api/runs`.                                                                                                                                                                                                                                                                            | Blocked for live-demo acceptance: the server path is wired, but Browserbase hosted MCP returned the same temporary model-capacity error at `extract`. The server reports a failed run instead of fabricating receipts.                                                                                                                                                                                                                                                                           | Hosted demo is not reviewer-ready until retries/rate limits and failed-run UX are accepted or Browserbase capacity stabilizes.                                                                                                               | Update docs and stop before deploy/post.   |
| 14   | 2      | Changed the shared MCP proof runner from streaming public writes to accepted-run publication.                                                                                   | Focused code inspection of `wrapped-mcp-proof-runner.ts`, Browserbase packet, and live demo server; patched local capture and explicit publish phase.                                                                                                                                                                                                                                                                                                                                                              | Accepted by design review: the wrapper now submits to a local capture log during the tool flow. Public publication happens only after all calls complete, record signatures verify, order checks pass, and private-material scan passes.                                                                                                                                                                                                                                                         | Firecrawl live rerun still needs a refreshed Firecrawl API key cache.                                                                                                                                                                        | Add regression test and regenerate proofs. |
| 15   | 2      | Added a regression test for failed public-mode packets.                                                                                                                         | `pnpm --filter @atrib/integration test -- test/mcp-platform-proof-packets.test.ts test/browserbase-stagehand-live-demo.test.ts`; focused `tsc --ignoreConfig ... --types node ...` over the changed runner and tests; Prettier check.                                                                                                                                                                                                                                                                              | Accepted: focused TypeScript passed; Vitest passed 2 files, 9 tests; Prettier passed. Package-level `pnpm --filter @atrib/integration typecheck` still fails on existing missing declarations for sibling `@atrib/*` packages, not this patch.                                                                                                                                                                                                                                                   | None for Browserbase structural fix.                                                                                                                                                                                                         | Regenerate Browserbase public packet.      |
| 16   | 2      | Regenerated the Browserbase public proof packet through the accepted-run publisher.                                                                                             | `BROWSERBASE_API_KEY=$(< ~/.atrib/secrets/browserbase-api-key) ATRIB_BROWSERBASE_STAGEHAND_LIVE=1 ATRIB_BROWSERBASE_UPSTREAM=hosted ATRIB_PACKET_PUBLIC_LOG=1 ATRIB_PACKET_WRITE_ARTIFACTS=1 pnpm --filter @atrib/integration browserbase-stagehand-packet`.                                                                                                                                                                                                                                                       | Accepted: six records verified with contiguous public log indexes `65792`, `65793`, `65794`, `65795`, `65796`, `65797`; verifier output includes `publish_policy: accepted-run-after-verification`.                                                                                                                                                                                                                                                                                              | None for Browserbase proof packet.                                                                                                                                                                                                           | Refresh Firecrawl cache.                   |
| 17   | 2      | Tried to refresh the Firecrawl cache from the provided 1Password item UUID.                                                                                                     | Removed zero-byte Firecrawl temp cache; `op signin --account=YMWE45M5BRCSZIN37BO4RC4JPE`; bounded `op read` attempts for `op://Private/jana7xbih7yw3b2tqhoukfflfa/password`; checked for inherited `FIRECRAWL_API_KEY` and local env files without printing values.                                                                                                                                                                                                                                                | Blocked: both 1Password accounts report not signed in for CLI use; `op signin` timed out; no `OP_SERVICE_ACCOUNT_TOKEN` or inherited `FIRECRAWL_API_KEY` is present. The Firecrawl cache remains absent.                                                                                                                                                                                                                                                                                         | Human action needed: authenticate 1Password CLI or seed `~/.atrib/secrets/firecrawl-api-key` through the established cache.                                                                                                                  | Stop before Firecrawl live rerun.          |
| 18   | 2      | Added bounded live retries, best-effort `end` cleanup, deployment guards, rate limits, Fly config, and deploy docs for Browserbase.                                             | Focused `tsc --ignoreConfig ...`; `pnpm --filter @atrib/integration test -- test/mcp-platform-proof-packets.test.ts test/browserbase-stagehand-live-demo.test.ts`; `pnpm doc-sync`; humanizer-style wording scan; `git diff --check`; secret-pattern scan.                                                                                                                                                                                                                                                         | Accepted: TypeScript passed; Vitest passed 2 files, 12 tests; doc-sync passed; prose scan clean; whitespace clean. Secret scan only hit redaction regexes and a deterministic fixture string.                                                                                                                                                                                                                                                                                                    | None for local Browserbase demo hardening.                                                                                                                                                                                                   | Run local live-demo proof.                 |
| 19   | 2      | Ran the Browserbase demo server locally in deployed-guard mode.                                                                                                                 | `BROWSERBASE_API_KEY=$(< ~/.atrib/secrets/browserbase-api-key) ATRIB_BROWSERBASE_DEMO_MODE=live ATRIB_BROWSERBASE_UPSTREAM=hosted ATRIB_BROWSERBASE_DEMO_PUBLIC_LOG=1 ATRIB_BROWSERBASE_DEMO_DEPLOYED=1 ATRIB_BROWSERBASE_DEMO_CREDENTIAL_SCOPE=demo-only ATRIB_BROWSERBASE_DEMO_URL=https://example.com ATRIB_BROWSERBASE_LIVE_MAX_ATTEMPTS=3 PORT=0 pnpm --filter @atrib/integration exec tsx examples/browserbase-stagehand/live-demo/server.ts`; `curl /health`; `curl /api/config`; `curl -X POST /api/runs`. | Accepted: local guarded server reported `deployment_ready: true`; one live hosted public-log run accepted with indexes `65840`, `65841`, `65842`, `65843`, `65844`, `65845`.                                                                                                                                                                                                                                                                                                                     | None.                                                                                                                                                                                                                                        | Deploy hosted demo.                        |
| 20   | 2      | Deployed Browserbase demo to Fly with demo-only secret policy.                                                                                                                  | `flyctl apps create atrib-browserbase-stagehand-demo`; `flyctl secrets set -a atrib-browserbase-stagehand-demo BROWSERBASE_API_KEY=...`; `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only`.                                                                                                                                                                                                                                                                  | First deployment succeeded and exposed `https://atrib-browserbase-stagehand-demo.fly.dev/`; config and health endpoints passed. A deployed `POST /api/runs` hung, and one Fly machine became unhealthy while the request was active.                                                                                                                                                                                                                                                             | Structural issue found: the server was spawning the package script, which rebuilt packages per request and could starve or hang a 512 MB Fly machine.                                                                                        | Fix deployed run path.                     |
| 21   | 2      | Fixed the deployed run path by invoking the packet script directly, adding a 120-second child-process timeout, and adding sanitized run logs.                                   | Focused `tsc --ignoreConfig ...`; `pnpm --filter @atrib/integration test -- test/mcp-platform-proof-packets.test.ts test/browserbase-stagehand-live-demo.test.ts`; `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only --ha=false`; `flyctl scale count 1 -a atrib-browserbase-stagehand-demo`; `curl /health`; `curl /api/config`; `curl -X POST /api/runs`; `flyctl logs`; `flyctl status`.                                                                   | Accepted: single Fly machine healthy; deployed config shows `deployment_ready: true`, public log on, rate limit `2/hour` and `8/day`, timeout `120000`; deployed live run accepted in about 10 seconds with indexes `65857`, `65858`, `65859`, `65860`, `65861`, `65862`; logs include `run_started` and `run_accepted`.                                                                                                                                                                         | None for Browserbase hosted demo.                                                                                                                                                                                                            | Update contact drafts, commit, and push.   |
| 22   | 2      | Converted the live demo to an in-process packet call, made `POST /api/runs` queue work asynchronously, kept the Fly machine warm, and changed container startup to direct Node. | Direct Node startup smoke with `node --import tsx .../server.ts`; focused `tsc --ignoreConfig ...`; `pnpm --filter @atrib/integration test -- test/mcp-platform-proof-packets.test.ts test/browserbase-stagehand-live-demo.test.ts`; local fixture HTTP smoke; `pnpm doc-sync`; `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only --ha=false`; deployed async `POST /api/runs`; `flyctl status`; `flyctl logs`.                                               | Accepted: TypeScript passed; Vitest passed 2 files, 13 tests; doc-sync passed; direct startup binds without the `pnpm --filter ... exec` wrapper; local `POST /api/runs` returned `202` in 2 ms; deployed `POST /api/runs` returned `202` in 130 ms and the polled live Browserbase run accepted all six operations with indexes `65925`, `65926`, `65927`, `65928`, `65929`, `65930`; Fly status shows one started machine with one passing health check and logs show the direct Node command. | The image is still 586 MB because `@atrib/integration...` installs unrelated example dependencies and downloads Playwright browsers. That does not affect reviewer-triggered run latency, but it is still a package-scope cleanup candidate. | Commit and push.                           |
| 23   | 2      | Changed the Browserbase demo Docker build to skip package postinstall scripts and rebuild only `esbuild`, avoiding local Playwright browser downloads.                          | Focused `tsc --ignoreConfig ...`; `pnpm --filter @atrib/integration test -- test/mcp-platform-proof-packets.test.ts test/browserbase-stagehand-live-demo.test.ts`; `pnpm doc-sync`; local fixture HTTP smoke; `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only --ha=false`; deployed async `POST /api/runs`; `flyctl status`; `flyctl logs`.                                                                                                                 | Accepted: TypeScript passed; Vitest passed 2 files, 13 tests; doc-sync passed; local `POST /api/runs` returned `202` in 24 ms; deploy image size dropped from 586 MB to 292 MB; deployed `POST /api/runs` returned `202` in 142 ms and the polled live Browserbase run accepted all six operations with indexes `65957`, `65958`, `65959`, `65960`, `65961`, `65962`; Fly status shows one started machine with one passing health check.                                                        | No active Browserbase demo blocker. A deeper image split could remove unrelated npm packages, but the browser runtime download and run-path lag are gone.                                                                                    | Commit and push.                           |
| 24   | 2      | Added browser-motion playback to the Browserbase demo, including typed visual run state, cursor and click overlay, allow/block visual states, UI-only Live View and Replay links, README boundaries, and contact-draft copy. | `pnpm --filter @atrib/integration build`; `pnpm --filter @atrib/integration exec vitest run test/browserbase-stagehand-live-demo.test.ts test/browserbase-action-control.test.ts test/mcp-platform-proof-packets.test.ts`; `pnpm doc-sync`; `git diff --check`; `pnpm --filter @atrib/integration browserbase-stagehand-packet`; Playwright against `http://127.0.0.1:18895` for allow, block, and mobile; humanizer/banned-prose scan. | Accepted: build passed; Vitest passed 3 files, 26 tests; doc-sync passed; whitespace passed; packet smoke signed six fixture records; Playwright confirmed allow clicks and leaves the target `approved`, block leaves the target `pending approval` without an `act` evidence row, and mobile has no horizontal overflow. Screenshots: `/tmp/atrib-browserbase-clicky-allow-page.png`, `/tmp/atrib-browserbase-clicky-block-page.png`, `/tmp/atrib-browserbase-clicky-mobile.png`. | No active visual-demo blocker. True Browserbase cloud Live View or Replay still depends on the hosted runtime providing UI-only refs. | Commit and push.                           |
| 25   | 2      | Ran the available UI and UX skill pass and tightened the Browserbase demo interaction layer: 44px controls, focus states, reduced motion, transform-only cursor motion, inert media-mode labels, and less placeholder-like target content. | Skill review against interface-design, web-design-guidelines, baseline-ui, high-end visual design, accessibility, make-interfaces-feel-better, Vercel React guidance, composition patterns, redesign-existing-projects, and design-review; focused `tsc --ignoreConfig ...`; focused Vitest; Playwright before and after audits on desktop, mobile, reduced motion, and embedded iframe states; final `pnpm --filter @atrib/integration build`; `pnpm doc-sync`; `git diff --check`; prose scan on `LOOP.md`. | Accepted: build passed; Vitest passed 3 files, 26 tests; doc-sync passed; whitespace passed; prose scan passed; post-run Playwright audit shows no horizontal overflow, no sub-44px focus targets, no disabled media buttons, cursor transitions only `transform, opacity`, reduced-motion transition duration `1e-06s`, and the settled target state is `approved`. Screenshots: `/tmp/atrib-browserbase-uiux-before.png`, `/tmp/atrib-browserbase-uiux-final-run.png`, `/tmp/atrib-browserbase-uiux-final-mobile.png`, `/tmp/atrib-browserbase-uiux-final-settled.png`. | No active UI or UX audit blocker. | Commit and push. |
| 26   | 2      | Reframed the demo from proof-console polish to staged browser playback: browser viewport first, evidence rail second, hidden JSON by default, real target highlights, and viewport-derived cursor coordinates. | Focused `tsc --ignoreConfig ...`; focused Vitest; `pnpm --filter @atrib/integration build`; `pnpm doc-sync`; `git diff --check`; Playwright desktop, approval-frame, mobile, and reduced-motion audits against `http://127.0.0.1:18922`. | Accepted: the approval frame shows `Click approve renewal`, target stage `act`, `approved=true`, JSON hidden with `display:none`, workflow summary hidden, no horizontal overflow, no sub-40px targets, cursor transitions only `transform, opacity`, and reduced-motion transition duration `1e-06s`. Screenshots: `/tmp/atrib-browserbase-clicky-final-approval.png` and `/tmp/atrib-browserbase-clicky-final-mobile.png`. | Boundary remains explicit: this is a staged webapp overlay over the existing proof path, not a browser extension or WebMCP polyfill package. | Commit and push. |
| 27   | 2      | Deployed the staged Browserbase playback UI to Fly. | `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only --ha=false`; `curl /health`; `curl /api/config`; `flyctl status`; hosted Playwright load-only UI audit against `https://atrib-browserbase-stagehand-demo.fly.dev/`. | Accepted: Fly deployed image `deployment-01KW84HQ0SSN2SYPJ3QA36QK66` at 292 MB; machine `0805454ae22338` is started with 1 passing health check; hosted config reports `mode=live`, `public_log=true`, `live_ready=true`, `deployment_ready=true`, and no guard issues; load-only Playwright confirmed the deployed page shows `remote browser | observe | gate | click | verify`, JSON hidden with `display:none`, summary hidden, no overflow, no sub-40px targets, and cursor transitions only `transform, opacity`. Screenshot: `/tmp/atrib-browserbase-hosted-deployed-initial.png`. | Hosted verification intentionally did not click `Run proof`, because that would create fresh public log records. | Stop. |
| 28   | 2      | Reworked the Browserbase demo contract around native Browserbase media: private tool-result observer, session media extraction, hash-only session refs, server-side replay proxy routes, Browserbase-first UI labels, and a responsive metadata strip. | Focused TypeScript; focused Vitest; `pnpm --filter @atrib/integration build`; focused 3-file Vitest; `pnpm doc-sync`; `git diff --check`; humanizer/banned-prose scan; in-app browser desktop and mobile rendered smoke against local fixture server. | Accepted after retry: initial rendered smoke found fixture-private replay refs being promoted as real media and metadata overlap at 1280px. Fixed both. Final checks passed: build, 28 focused tests, doc-sync, whitespace, prose scan, desktop rendered run, and mobile layout. Browser smoke showed fixture media remains `Browserbase media pending`, JSON stays hidden, no uppercase `Atrib`, no console errors, no horizontal overflow, and no sub-40px targets. | Live Browserbase replay proxy appears only when a real live run exposes a Browserbase session id. The hosted MCP may still not return that ref. | Commit, push, and deploy if accepted. |
| 29   | 2      | Deployed the native Browserbase media demo and removed the noisy WebMCP `Permissions-Policy` header from the target page. | `curl /health`; `curl /api/config`; `flyctl status`; hosted desktop and mobile Playwright load-only smoke; focused TypeScript; focused Vitest; 3-file focused Vitest; `pnpm --filter @atrib/integration build`; `pnpm doc-sync`; `git diff --check`; humanizer/banned-prose scan; redeploy with `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only --ha=false`; hosted endpoint and browser smoke after redeploy. | Accepted after retry: initial hosted smoke found Chromium warning `Unrecognized feature: 'tools'` from our explicit `Permissions-Policy: tools=(self)` header. The target is top-level and WebMCP defaults the tools policy to self, so the header was removed. Final hosted smoke passed on 1280px desktop and 390px mobile: no console warnings or errors, no horizontal overflow, no sub-40px targets, lowercase `atrib`, health ok, config live and deployment-ready, Fly image `deployment-01KW87HDZA5S4MFMDZHBTTCFDN`, machine version 7 with 1 passing health check. | Hosted verification intentionally did not click `Run proof`, because the deployed app is in live public-log mode. Browserbase Live View or Replay still appears only when a live run exposes a private session ref. | Commit and push deployment receipt. |
| 30   | 2      | Made Browserbase media modes inspectable after real runs: hydrate the newest run on page load and render Live, Replay, and Simulated as real buttons. | UI-clicked hosted `Run proof`; replay proxy metadata and HLS checks; focused TypeScript; focused Vitest; local fixture browser smoke for run reload and media buttons; fresh local reload smoke; 3-file focused Vitest; `pnpm --filter @atrib/integration build`; `git diff --check`; `pnpm doc-sync`; humanizer/banned-prose scan; redeploy with `flyctl deploy -c packages/integration/examples/browserbase-stagehand/live-demo/fly.toml --remote-only --ha=false`; hosted browser e2e after redeploy. | Accepted after retry: first local browser smoke showed the media controls were rendered as buttons but had `role="listitem"`, hiding them from button-role queries. Removed the role. Final hosted e2e clicked `Run proof`, accepted run `bb-mqyfczgq-u7z9zg` in 9.373s, wrote tool indexes `67553`-`67558` plus policy indexes `67559`-`67560`, reloaded the page, selected Replay by default, showed session hash `sha256:7f662d04c5bf...3dcef739`, exposed `/api/runs/bb-mqyfczgq-u7z9zg/browserbase/replays`, returned replay metadata with `pageCount=1`, and returned a valid HLS playlist from the per-page proxy. Hosted UI had no console errors, no horizontal overflow, no uppercase `Atrib`, and timeline policy rows remained visible. | Live View remains unavailable because the hosted MCP result exposed a session id and replay metadata, not a Live View URL. Replay works through the server proxy. | Commit and push. |
