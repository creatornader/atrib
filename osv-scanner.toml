[[IgnoredVulns]]
id = "GHSA-jmr9-qjv8-65gv"
ignoreUntil = 2026-11-24
reason = "extract-zip has no patched release. It is a private integration-only transitive dependency; browser-use rejects traversal and symbolic-link archive entries. scripts/check-extract-zip-exception.mjs fails when npm publishes a new release."

[[IgnoredVulns]]
id = "GHSA-7pqw-9j4j-h8q3"
ignoreUntil = 2026-12-14
reason = "extract-zip has no patched release. The only repository consumer is the private browser-use integration path, which extracts into a fresh 0700 temp directory, rejects archive symlink entries, rejects traversal and absolute paths, and caps entries and uncompressed bytes."

[[IgnoredVulns]]
id = "GHSA-vwc7-r8mq-g2x9"
ignoreUntil = 2026-12-14
reason = "adm-zip has no patched release. It is a private integration-only dependency; the reachable browser-use callsite creates in-memory DOCX archives with addFile/toBuffer and does not call extraction APIs or write archive entries to disk."
