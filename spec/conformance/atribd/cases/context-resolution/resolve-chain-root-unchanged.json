{
  "name": "resolve-chain-root-unchanged",
  "spec_section": "P046",
  "check": "chain-root-corpus",
  "description": "Interaction vector: the daemon consumes resolveChainRoot unmodified. Every case in the §1.2.3 multi-producer corpus MUST resolve to the same chain_root when run from the daemon's dependency surface. The daemon never reimplements chain selection.",
  "input": {
    "reuses_corpus": "spec/conformance/1.2.3/multi-producer"
  },
  "expected": {
    "all_cases_match": true
  }
}
