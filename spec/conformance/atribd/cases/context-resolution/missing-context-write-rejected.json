{
  "name": "missing-context-write-rejected",
  "spec_section": "D147",
  "check": "http-carrier",
  "description": "Rung 3: nothing resolves, so the write primitive returns a typed tool error. This is today's ATRIB_REQUIRE_EXPLICIT_CONTEXT_ID=1 behavior promoted to the HTTP default.",
  "input": {
    "body": {
      "jsonrpc": "2.0",
      "id": 5,
      "method": "tools/call",
      "params": {
        "name": "emit",
        "arguments": {
          "content": {
            "what": "orphan"
          }
        }
      }
    }
  },
  "expected": {
    "rejected": true,
    "error_text": "atrib: context_id required on stateless transport"
  }
}
