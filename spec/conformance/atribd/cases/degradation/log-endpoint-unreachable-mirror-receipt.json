{
  "name": "log-endpoint-unreachable-mirror-receipt",
  "spec_section": "D147",
  "description": "§5.8: with the log endpoint unreachable, a write-primitive call still returns a signed record_hash and the record lands in the local mirror. Submission is queued and retried in the background; the primary call never blocks on network state.",
  "input": {
    "tool": "emit",
    "arguments": {
      "event_type": "observation",
      "context_id": "5e11ab1e5e11ab1e5e11ab1e5e11ab1e",
      "content": {
        "what": "degradation vector: unreachable log endpoint"
      }
    },
    "log_endpoint": "http://127.0.0.1:0/v1/entries"
  },
  "expected": {
    "record_hash_signed": true,
    "mirror_line_written": true,
    "warnings_contains": "submission queued"
  }
}
